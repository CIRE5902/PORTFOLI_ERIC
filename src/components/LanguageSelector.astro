<header class="top-bar">
  <div class="icon-wrapper" data-tooltip="Idioma">
    <div class="language-selector" id="languageSelector">
      <div class="selected-language" id="selectedLanguage">
        <img src="/images/ingles.png" alt="Bandera de Español" />
      </div>
      <div class="language-options">
        <div data-lang="en">
          <img src="/images/ingles.png" alt="Bandera de Inglés" />
        </div>
        <div data-lang="es">
          <img src="/images/espana.png" alt="Bandera de Español" />
        </div>
        <div data-lang="ca">
          <img src="/images/catala.png" alt="Bandera de Catalán" />
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  .top-bar {
    display: flex;
    justify-content: flex-end;
    padding: 1rem;
    background-color: var(--bg-header);
    position: sticky;
    top: 0;
    z-index: 1000;
  }

  .icon-wrapper {
    position: relative;
    cursor: pointer;
  }

  .language-selector {
    position: relative;
    width: 40px;
    height: 40px;
  }

  .selected-language {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    overflow: hidden;
    border: 2px solid var(--primary-color);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: border-color 0.3s;
  }

  .selected-language img {
    width: 100%;
    height: auto;
    object-fit: cover;
  }

  .language-options {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 0.5rem;
    background-color: var(--bg-light);
    border-radius: 0.5rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    padding: 0.5rem;
    display: none;
    flex-direction: column;
    gap: 0.5rem;
    z-index: 100;
  }

  .language-options div {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    overflow: hidden;
    cursor: pointer;
    transition: transform 0.2s ease;
  }

  .language-options div:hover {
    transform: scale(1.1);
  }

  .language-options img {
    width: 100%;
    height: auto;
    object-fit: cover;
  }

  .language-selector:hover .language-options {
    display: flex;
  }

  [data-tooltip]::after {
    content: attr(data-tooltip);
    position: absolute;
    top: -30px;
    right: 0;
    background: #000;
    color: #fff;
    padding: 4px 8px;
    font-size: 0.75rem;
    border-radius: 4px;
    opacity: 0;
    transform: translateY(5px);
    transition: all 0.2s ease;
    pointer-events: none;
    white-space: nowrap;
  }

  [data-tooltip]:hover::after {
    opacity: 1;
    transform: translateY(0);
  }
</style>
